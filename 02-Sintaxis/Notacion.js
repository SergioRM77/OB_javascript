//Notación

// ; --> delimitar final de linea, ya no es obligatorio pero es una buena práctica
const b = 3;

// [] -->se usa para listas, arreglos, arrays...
const ar = [1,2,3,4];
console.log(ar[3]); //4

// () --> funciones
function suma(a,b){}


// {} --> llaves para objetos, funciones y estructuras de control
const movil = {
    ancho : 3,
    alto: 10
}

// . --> se utiliza en los objetos para acceder a los atributos
movil.ancho;// no se puede acceder antes de declararlo