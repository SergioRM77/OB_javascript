/**
 * creamos una plantilla html, un archivo index.js con datos a mostrar y dentro del <head> el <script src="index.js"> para llamarlo
 * terminal> npm init -y para crear un package.json por defecto y dentro, un script con "start": "http-server" .
 * terminal> npm install --global http-server para descargar ficheros que activen el http-server
 * terminal> npm start para activar el servicio http-server
 * 
 */
const a = 4;

const b = 7;

console.log(a*b);

const p = document.getElementById("parrafo");
console.log("hola");
console.log(p);